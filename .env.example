# Frontend environment variables.
# Copy to `.env.local` and adjust to point at your backend.

# Base URL of the FastAPI backend. The frontend prepends this to every
# `/api/...` path. In production set this to e.g. https://api.your-domain.ru.
VITE_API_URL=http://localhost:8000
